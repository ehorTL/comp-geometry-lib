import { ElementCreatedDirective } from './element-created.directive';

describe('ElementCreatedDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementCreatedDirective();
    expect(directive).toBeTruthy();
  });
});
